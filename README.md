# docker-ubuntu-toolbox
A Dockerfile to make it easier to live on other OS

## Usage
Create an image

```
$ make image
```

Push the image to Docker Hub

```
$ make push
```

Use the image

```
$ make run
```
